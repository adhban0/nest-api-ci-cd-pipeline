services:
  nest-api-prod:
    container_name: nest-api-prod
    image: sadeeee/nest-api-ci-cd-pipeline:latest
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      DB_PORT: 1001
    
    profiles:
      - production
