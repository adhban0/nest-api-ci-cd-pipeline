name: Build and Deploy

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  
jobs:    
  Build-Push:
    if: github.event.base_ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Detect application changes
        id: changes
        uses: dorny/paths-filter@0e4a8c6effa4802afeda77dc8d303f8176d7dfad # v3
        with:
          predicate-quantifier: 'every'
          filters: |
            app:
              - '**'
              - '!docker-compose.yml'
              - '!.github/workflows/main.yml'

      - name: Set up Docker Buildx
        if: github.event_name == 'workflow_dispatch' || steps.changes.outputs.app == 'true'
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3

      - name: Log in to GHCR
        if: github.event_name == 'workflow_dispatch' || steps.changes.outputs.app == 'true'
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@818d4b7b91585d195f67373fd9cb0332e31a7175 # v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=semver,pattern={{version}}

      - name: Build and push Docker image
        uses: docker/build-push-action@0a97817b6ade9f46837855d676c4cca3a2471fc9 # v4
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # Deploy:
  #   needs: Build-Push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Configure SSH key
  #       run: |
  #         mkdir -p ~/.ssh
  #         printf '%s' "${{ secrets.DEPLOY_KEY }}" > ~/.ssh/id_ed25519
  #         chmod 600 ~/.ssh/id_ed25519

  #     - name: Deploy to server
  #       run: |
  #         echo "Starting SCP..."
  #         scp -v \
  #           -i ~/.ssh/id_ed25519 \
  #           -o StrictHostKeyChecking=no \
  #           -o UserKnownHostsFile=/dev/null \
  #           docker-compose.yml \
  #           ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }}:/opt/docker/${{ github.event.repository.name }}/

  #         echo "Starting reload..."
  #         ssh \
  #           -i ~/.ssh/id_ed25519 \
  #           -o StrictHostKeyChecking=no \
  #           -o UserKnownHostsFile=/dev/null \
  #           ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} \
  #           "sudo systemctl reload docker-compose@${{ github.event.repository.name }}.service"